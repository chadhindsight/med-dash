import React from 'react';

const InputEdit = (props) => {
    return (
        <div>
            {/* Get these attr from props */}
            {/* <input name="email" placeholder="email" onChange={props.onUserEdit} 
            disabled={props.edit[props.name+ 'Edit']}/> */}
        </div>
    );
};

export default InputEdit;